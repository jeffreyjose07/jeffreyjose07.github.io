# Everything here is public and crawlable.
User-agent: *
Allow: /

# Sitemap must name the canonical host — the URLs inside it are all on
# is-a.dev, and a sitemap declared on a different host is treated as
# cross-domain and ignored.
Sitemap: https://jeffreyjose07.is-a.dev/sitemap.xml
